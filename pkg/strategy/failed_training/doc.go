// Package failed_training archives strategies that failed the backtest bar
// (win rate under 50%, CAGR under 10%, or zero trades). The .go files here
// are tagged //go:build ignore so they are not registered.
package failed_training
